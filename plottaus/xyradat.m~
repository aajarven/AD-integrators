colors = 
colorsRepeated = repmat([255 153 0; 255 102 0; 0 153 0; 204 0 0; 0 0 204; 0 0 0]/255, length(index)/6);
colors = colors(:,1:3);
sizes = repmat([40; 2; 2; 2; 2; 2], length(index)/6);
sizes = sizes(:,1);

scatter(x, y, sizes, colors, 'filled')

xlabel('x');
ylabel('y');

! legend
hold on;
h = zeros(6, 1);
for i = 1:6
    h(i) = scatter(NaN, NaN, colors(i), 'filled');
end
legend(h, 'Sun','Jupiter','Saturn', 'Uranus', 'Neptune', 'Pluto');